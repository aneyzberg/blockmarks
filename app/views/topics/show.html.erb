<h1><%= @topic.name %></h1> 

<div class="row">
  <div class="col-md-8">
    <div class="table">
      <tr>
        <% @topic.bookmarks.each do |bookmark| %>
        <td> <%= link_to bookmark.url, bookmark %></td> <br />
      </tr>
      <% end %>
    </div>
   </div>
  </div> 
