<h1> My Boomarks </h2>
<div class="row">
  <div class="col-md-8">
      <div class="row">
            <% @bookmarks.current_user.each do |bookmark| %>
      <%= link_to bookmark.url, bookmark %><br />
         <% end %>
    </div>
  </div>
</div>

<h1> My Likes </h2>
  <div class="row">
     <div class="col-md-8">
      <div class="row">
      <% @bookmarks.cuurent_user.likes.each do |bookmark| %>
      <%= link_to bookmark.url, bookmark %><br />
      <% end %>

    </div>
  </div>
</div>
